apiVersion: traefik.io/v1alpha1
kind: IngressRoute
metadata:
  name: default-frontend
  namespace: k8s-test-app
spec:
  entryPoints:
    - web
    - websecure
  routes:
    - match: PathPrefix(`/`)
      kind: Rule
      services:
        - name: frontend
          port: 80
